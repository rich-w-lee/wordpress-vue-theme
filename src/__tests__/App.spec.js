import { shallowMount } from '@vue/test-utils';
import App from '../App';

describe('App.vue', () => {
  let wrapper;

  beforeEach(() => {
    // wrapper = shallowMount(App);
  });

  it('renders the component', () => {

  });
});
